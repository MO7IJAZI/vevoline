# ملف متغيرات البيئة لإعدادات Hostinger (الإنتاج)
# --------------------------------------------------
# ملاحظة: يجب إدخال هذه المتغيرات في لوحة تحكم Hostinger (قسم Node.js > Environment Variables)

# 1. إعدادات قاعدة البيانات (PostgreSQL عبر Neon أو أي مزود مُدار)
# الصيغة: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
# مثال Neon: postgresql://<user>:<password>@<project>.neon.tech/<db>?sslmode=require
# DATABASE_URL="postgresql://neondb_owner:npg_1vEc2qARzaeF@ep-tiny-dream-aihq9oyy-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require"
DB_DIALECT=mysql
SESSION_STORE=mysql
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=u884871213_vevo
MYSQL_PASSWORD=VevoVevo1213
MYSQL_DATABASE=u884871213_vevo
SESSION_SECRET=asdqwezxc

# 2. إعدادات الجلسة والأمان
# قم بتغيير SESSION_SECRET إلى كلمة سر طويلة وعشوائية جداً
SESSION_SECRET="dev_secret_key_123"
NODE_ENV=production
PORT=3000

# 3. إعدادات المدير (Admin)
# هذه البيانات ستستخدم عند تشغيل كود الـ Seeding لأول مرة
ADMIN_EMAIL=admin@vevoline.com
ADMIN_PASSWORD=admin123

# 4. إعدادات البريد الإلكتروني (SMTP)
# يفضل استخدام بريد Hostinger الرسمي أو Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=vevoline00@gmail.com
FROM_EMAIL=vevoline00@gmail.com
SMTP_PASS=hgmyoeuttdxocwrp
FROM_NAME=VevoLine dashboard
# 5. إعدادات التطبيق
# يجب وضع الرابط النهائي للموقع (بدون / في النهاية)
APP_URL=https://vevoline.space

# 6. إعدادات الملفات (Uploads)
# في Hostinger، المسار عادة ما يكون نسبي لمجلد المشروع
PRIVATE_OBJECT_DIR=./uploads
